#ifndef ESP32_COMPAT_H
#define ESP32_COMPAT_H

// Это определение должно быть строго таким же, как требует компилятор во второй компиляции
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"

// Никаких своих объявлений не добавляем
// Просто используем определения из ESP-IDF

#endif // ESP32_COMPAT_H